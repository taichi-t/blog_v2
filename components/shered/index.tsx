import { css } from '@linaria/core';

const BoxShadow = css`
  box-shadow: 5px 6px 0 var(--color-border);
  border-radius: 3px;
`;

const Border = css`
  border: 2px solid var(--color-border);
  border-radius: 3px;
`;

export { BoxShadow, Border };
