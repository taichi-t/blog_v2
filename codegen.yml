schema: ${GRAPHQL_SCHEMA_PATH}
overwrite: true
generates:
  ./generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
hooks:
  afterOneFileWrite:
    - prettier --write
